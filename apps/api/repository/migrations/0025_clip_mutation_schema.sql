-- Clip mutation operations extend the closed command union without changing
-- current projection tables. The paired data migration advances canonical
-- proposal and transaction domains from v2 to v3 and recomputes their digests.
SELECT 1;
